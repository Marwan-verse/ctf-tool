name: remanence

services:
  backend:
    build:
      context: ./backend
    environment:
      REMANENCE_ALLOWED_HOSTS: "localhost,127.0.0.1,[::1],backend,testserver"
      REMANENCE_ALLOWED_ORIGINS: "http://localhost:3000,http://127.0.0.1:3000"
      REMANENCE_DATA_DIR: /data
      REMANENCE_DB_PATH: /data/remanence.sqlite3
      REMANENCE_MAX_UPLOAD_BYTES: ${REMANENCE_MAX_UPLOAD_BYTES:-104857600}
      REMANENCE_MAX_WORKERS: ${REMANENCE_MAX_WORKERS:-2}
      PYTHONUNBUFFERED: "1"
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - remanence-data:/data
    networks:
      - remanence-local
    read_only: true
    tmpfs:
      - /tmp:rw,nosuid,nodev,noexec,size=256m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    mem_limit: 2g
    cpus: 2.0
    stop_grace_period: 20s

  web:
    build:
      context: ./web
    environment:
      HOST: 0.0.0.0
      NEXT_PUBLIC_API_URL: http://localhost:8000
      PORT: "3000"
    ports:
      - "127.0.0.1:3000:3000"
    networks:
      - remanence-local
    depends_on:
      backend:
        condition: service_healthy
    init: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    mem_limit: 1g
    cpus: 1.0
    stop_grace_period: 15s

networks:
  remanence-local:
    internal: true

volumes:
  remanence-data:
